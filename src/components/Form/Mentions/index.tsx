import { Mentions as OriginalMentions } from 'antd';

import withFinalForm from '../withFinalForm';

export const Mentions = withFinalForm(OriginalMentions);
export const Option = OriginalMentions.Option;

export default Option;
